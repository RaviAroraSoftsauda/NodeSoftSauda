{{> header}}
{{> nav}}
<div id="page-wrapper">
    <div class="row">
         <div class="col-lg-12">
            <a href="/direct_delivery_entry/direct_delivery_entry" style="color:white"><h1 class="btn btn-primary pull-left" style="margin-bottom: 21px;">Add Direct Delivery Entry</h1></a>
             {{!-- <button type="submit" class="btn btn-primary pull-right"> Submit </button> --}}
        </div>
            {{!-- <div class="panel panel-default"> --}}
                {{!-- <div class="panel-heading"> --}}
                    {{#if err}}
                        <div class="alert alert-danger">
                            <strong>{{err}}</strong>
                        </div>
                    {{/if}}
                    {{#if errors}}
                             <div class="alert alert-danger">
                                <strong>{{errors}}</strong>
                            </div>
                    {{/if}}
                {{!-- </div> --}}
      {{!-- </div> --}}
        <div class="col-lg-12" style="margin-top: 22px;">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <i class="fa fa-bell fa-fw"></i>Direct Delivery List
                </div>
                <div class="table-responsive">
                    <table class="product_detail_table ">
                        {{!-- {{contract}} --}}
                        <thead>
                        <tr>
                            <th>Sno.</th>
                            <th>Delivery</th>
                            <th>Date</th>
                            <th>Term & Condition</th>
                            <th>Edit</th>
                            <th>Delete</th>
                      </tr>  
                        </thead>
                        <tbody>
                            {{!-- {{contract}} --}}
                                {{#each delveryentry1 as |value key|}}
                                <tr>
                                    <td>{{sum @index 1}}</td>
                                    <td>{{this.c_j_s_p}}</td>
                                    <td>{{dateformat value.sd_date 'DD/MM/YYYY'}}</td>
                                    <td>{{this.party_terms.narration_name}}</td>
                                    <td><a href="/direct_delivery_entry/direct_entry_list_update/{{value._id}}"><i class="fa fa-pencil"></i></a></td>
                                    <td><a href="/direct_delivery_entry/delete_direct_delivery_entry/{{value._id}}" onclick="return confirm('Are you sure want to delete.');"><i class="fa fa-trash-o" style="color:red;"></i></a></td>
                               </tr>
                              {{/each}}  
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                </div>
        </div>  
        </div>
        {{> footer}}